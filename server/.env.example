PORT=5000
CLIENT_URL=lablens-plus-de7i.vercel.app

# Toggle between the offline TrOCR pipeline and Google Gemini.
# "true"  -> analyze prescription images via the Gemini service
# "false" -> use the offline TrOCR + Fuse.js pipeline (default fallback)
USE_GEMINI=false
